plugins {
    id "com.moowork" version "1.3.1"
    id "base"
}

group = 'org.projet_iwa'

apply plugin: "com.moowork.node"
apply plugin: "base"

task appNpmBuild(type: NpmTask) {
    args = [“run”, “build”]
}

assemble.dependsOn(appNpmBuild)

